{
  "name": "udemy01",
  "version": "1.0.0",
  "description": "",
  "main": "intersectionType.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "b": "npm run build",
    "dev": "npx concurrently -k -n COMPILER,NODEMON -c yellow,blue \"tsc -w\" \"nodemon -w build -q build/index.js\"",
    "dev2": "npx concurrently -k -n COMPILER,NODEMON -c yellow,blue \"tsc -w\" \"nodemon -w build -q build/intersectionType.js\"",
    "start": "node build/index.js",
    "s": "npm run start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/websocket": "^1.0.2",
    "concurrently": "^5.3.0",
    "nodemon": "^2.0.6",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "websocket": "^1.0.33"
  }
}
